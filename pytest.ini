[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    e2e: End-to-end browser tests (require Playwright)
    live: Calls a real third-party API; never on a gate target — run with make test-contract
