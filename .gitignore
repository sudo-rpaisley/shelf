data/
data-dev/
.env
__pycache__/
.devdocs
.coverage
reports/
htmlcov/
.claude
.codex/
.agents/
# Node build tooling (tailwind CLI) — see package.json
node_modules/
.temp/
# Pipeline scratch — recon notes a stage leaves behind. Untracked and NOT
# ignored, these are invisible to the release parity gate (both trees lack
# them, so the diff is empty and they silently never ship). Archive anything
# worth keeping under .devdocs/archive/completed/.
.session/
